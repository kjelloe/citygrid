{
  "name": "citygrid",
  "version": "0.0.0",
  "private": true,
  "description": "A city-building simulation for browsers and phones",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test 'test/**/*.test.js'"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
